# Installed into the app's private python_env on first run (see
# server_launcher.py). torch/torchvision are installed separately there
# with the CUDA wheel index — not listed here.
fastapi
uvicorn[standard]
python-multipart
pillow
transformers>=4.45
accelerate
qwen-vl-utils

# Label printing — Brother QL-800 over direct USB (brother_ql), NOT through
# a Windows print driver or P-touch Editor. Requires a one-time driver swap
# on the PC using Zadig — see the printing section in README.md.
brother_ql
pyusb
