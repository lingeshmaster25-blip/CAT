# Installed into the app's private python_env on first run (see
# server_launcher.py). torch/torchvision are installed separately there
# with the CUDA wheel index — not listed here.
fastapi
uvicorn[standard]
python-multipart
pillow
transformers>=4.45
accelerate
qwen-vl-utils

# Label printing — sent through the normal Windows print system (the
# Brother QL-800 is installed as a regular Windows printer here and prints
# correctly through it, confirmed). No raw-USB library needed.
pywin32
python-barcode
